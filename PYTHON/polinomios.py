def polinomio():
    a=input("Dame el valor de a: ")
    b=input("Dime el valor de b: ")
    c=input("Dime el valor de c: ")
    contenido_raiz=b**2-4*a*c
    if(contenido_raiz<0):
        print "No corta con el eje. Caso C"
    else:
        if(contenido_raiz==0)
        x=(-b+math.sqrt(contenido_raiz))/2*a


    if ((x>0 and z<0) or (x<0 and z>0)):
        print "El polinomio tiene una solucion"
    if (x<0 and z<0):
        print "El polinomio no tiene solucion"
    if (n_1==0 and n_2==0):
        print "El polinomio tiene dos soluciones"
polinomio()
